---
id: EV-W00-E01-S002-03
type: evidence
title: SD-03 confirmation — bench-budgets.txt reflects the #25 recalibration
status: recorded
created_at: 2026-07-13
updated_at: 2026-07-13
derived: false
---

# EV-W00-E01-S002-03 — SD-03 confirmation — bench-budgets.txt reflects the #25 recalibration

## Evidence ID

EV-W00-E01-S002-03.

## Evidence type

Inspection note (entry count, header provenance, spot-check vs commit 0a31186 diff) — inspection-based, not test-execution-based.

## Story and task

W00-E01-S002 / W00-E01-S002-T003.

## Acceptance criteria proven

AC-W00-E01-S002-03.

## Execution command

Manual inspection of `bench-budgets.txt` (path reconfirmed via `git ls-files '*bench-budgets*'` — repository root, sole match) + `git show 0a31186 -- bench-budgets.txt` + `git diff 0a31186 -- bench-budgets.txt` + `git status --porcelain bench-budgets.txt`.

## Code revision or commit SHA

`0a31186cada5c275a588c74081cf977adf346e61` (which is itself PR #25's merge commit — the SD-03 fix commit).

## Branch or tag

`main`.

## Execution environment

Local development machine — macOS 26.5.2 (Darwin 25.5.0), arm64 (Apple Silicon M3 Max). Machine shared with sibling W00 verification workers running tests concurrently — concurrent load present during timing-sensitive (benchmark) captures.

## Relevant tool versions

go1.26.5 darwin/arm64; GNU Make 3.81; git 2.55.0; benchbudget tool run from source at the same SHA via `go run ./internal/tools/benchbudget`.

## Date and time

2026-07-13, ~12:16 +05:30.

## Result

**Pass.**
- Entry count: **43** non-comment, non-blank lines — matches the expected "43 budgeted entries" figure. Reconciled with the tool's own view: the `make bench-budget` run printed exactly 43 `OK` result lines (one per budgeted entry).
- HEAD **is** commit `0a31186` (PR #25); `git diff 0a31186 -- bench-budgets.txt` is empty and the working tree is clean — the file is byte-identical to the post-#25 recalibrated state, with no later uncaptured drift possible.
- Spot-check (3 entries) vs the #25 diff: `BenchmarkTokenBucketSweepAt10k 4500000 0` (was 250000), `BenchmarkTokenBucketSweepAt100k 66000000 0` (was 2900000), `BenchmarkTokenBucketAllow 300 0` (unchanged context) — all match. Header carries the post-#25 annotation "(remeasured 2026-07-12: full-map sweep per iteration)".
- Live corroboration: today's measured sweep values (451615 / 7636231 ns/op) are the same order as #25's honest baselines (433262 / 6605698 ns/op) and inconsistent with the pre-#25 empty-map artifacts (23492 / 280212 ns/op).
- RISK-W00-003 (stale pre-#25 baseline) does **not** materialize.

## File or URI

`artifacts/T003-bench-budgets-inspection-note.md` (story-relative).

## Checksum where appropriate

sha256(T003-bench-budgets-inspection-note.md) = `649fadfb395ce4bef3686550660320fbec12a1751040a4459aad8ac057cb16c3`

## Reviewer

Pending — conductor acceptance gate (worker must not self-accept per W00 status discipline).

## Superseded evidence where applicable

None — first evidence record for this AC.
