---
id: EV-W01-E02-S002-002
type: evidence
title: Statement-summary / rows-affected / error-marking test
status: recorded
created_at: 2026-07-13
updated_at: 2026-07-13
derived: false
---

# EV-W01-E02-S002-002 — Statement-summary, rows-affected, error marking

## Evidence ID

EV-W01-E02-S002-002.

## Evidence type

Integration-test report (real Postgres + in-memory span exporter).

## Story and task

W01-E02-S002 / W01-E02-S002-T001.

## Acceptance criteria proven

AC-W01-E02-S002-02 (attrs + error-marking portions; literal-leakage portion in EV-W01-E02-S002-003).

## Execution command

`DATABASE_URL='postgres://wowapi:wowapi-local-only@localhost:5432/wowapi?sslmode=disable' go test ./kernel/database/ -run 'TestIntegrationQueryTracer' -v -race -count=1`

- `TestIntegrationQueryTracerStatementAndRowsAffectedAttrs` — `db.statement == "SELECT 1"`,
  `db.rows_affected == "1"` on the exported child span.
- `TestIntegrationQueryTracerMarksFailedQueryErrored` — a query against a missing table fails; the
  exported span has status `codes.Error` and a recorded `exception` event (the otel adapter's
  `RecordError` path).

## Code revision or commit SHA

`0a31186cada5c275a588c74081cf977adf346e61` (HEAD) plus this story's uncommitted working change.

## Branch or tag

`main` (uncommitted working tree).

## Execution environment

Local development machine — macOS (Darwin 25.5.0), arm64 Apple M3 Max; Postgres 16-alpine in
Docker (compose stack).

## Relevant tool versions

go1.26.5 darwin/arm64; postgres:16-alpine.

## Date and time

2026-07-13, ~07:33 UTC.

## Result

**Pass.** Both tests pass under `-race`. Failed-query spans are marked errored via the port's
`RecordError`; successful spans carry both attrs with the expected values.

## File or URI

`tests/ev-001-fail-first-after.txt` (same transcript, named tests within).

## Checksum where appropriate

sha256(ev-001-fail-first-after.txt) = `a305275b12c080cce5589e0fb32b8aac79833325a6329757ba7519ad6472f9b1`

## Reviewer

Pending — conductor acceptance gate.

## Superseded evidence where applicable

None — first evidence record for this AC portion.
