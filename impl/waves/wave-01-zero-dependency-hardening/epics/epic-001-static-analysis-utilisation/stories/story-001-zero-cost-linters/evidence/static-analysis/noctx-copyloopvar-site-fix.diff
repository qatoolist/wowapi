# noctx named-site fix evidence (AC-W01-E01-S001-02) — 2026-07-13, W01Lint
# DRIFT: noctx v2.11.4 does NOT report exec.Command sites (it checks net/http +
# database/sql only), so the story's fail-before/pass-after cannot be produced
# via noctx itself. The same sites WERE flagged by gosec G204 (fail-before in
# both triage runs); fix evidence = this code diff + gosec pass-after.

diff --git a/internal/cli/config_delegate.go b/internal/cli/config_delegate.go
index 33d4f8b..4c28686 100644
--- a/internal/cli/config_delegate.go
+++ b/internal/cli/config_delegate.go
@@ -8,6 +8,7 @@
 package cli
 
 import (
+	"context"
 	"encoding/json"
 	"errors"
 	"flag"
@@ -31,7 +32,10 @@ func delegateConfigCheck(mode string, args []string, stdout, stderr io.Writer) (
 		return false, 0 // no product checker → framework-only handling
 	}
 	runArgs := append([]string{"run", "./tools/configcheck", mode}, args...)
-	cmd := exec.Command("go", runArgs...)
+	// Deliberate subprocess: the CLI's job here IS to run the product's own
+	// configcheck via the local go toolchain with caller-supplied args (W01-E01
+	// gosec triage). Context-bound so the launch is cancellable in principle.
+	cmd := exec.CommandContext(context.Background(), "go", runArgs...) // #nosec G204 -- runs the repo's own `go run ./tools/configcheck`; args are the CLI caller's own
 	cmd.Stdout = stdout
 	cmd.Stderr = stderr
 	if err := cmd.Run(); err != nil {
diff --git a/internal/cli/lint_cmd.go b/internal/cli/lint_cmd.go
index 8d58bd0..99e1d24 100644
--- a/internal/cli/lint_cmd.go
+++ b/internal/cli/lint_cmd.go
@@ -12,6 +12,7 @@ package cli
 
 import (
 	"bytes"
+	"context"
 	"flag"
 	"fmt"
 	"io"
@@ -126,7 +127,9 @@ func runLintBoundaries(args []string, stdout, stderr io.Writer) int {
 
 // listImports runs `go list` and returns package → production import paths.
 func listImports(pattern string) (map[string][]string, error) {
-	cmd := exec.Command("go", "list", "-f", "{{.ImportPath}}: {{join .Imports \" \"}}", pattern)
+	// Deliberate subprocess: boundary lint shells out to the local go toolchain
+	// by design (W01-E01 gosec triage). Context-bound so it is cancellable.
+	cmd := exec.CommandContext(context.Background(), "go", "list", "-f", "{{.ImportPath}}: {{join .Imports \" \"}}", pattern) // #nosec G204 -- fixed `go list` argv; only the package pattern varies, supplied by the CLI caller
 	var errBuf bytes.Buffer
 	cmd.Stderr = &errBuf
 	out, err := cmd.Output()

# copyloopvar named-site fix (AC-W01-E01-S001-03):
diff --git a/app/maintenance.go b/app/maintenance.go
index d2636ca..c1d33f4 100644
--- a/app/maintenance.go
+++ b/app/maintenance.go
@@ -145,7 +145,6 @@ func registerMaintenance(sched *jobs.Scheduler, k *kernel.Kernel, slaEvery, idem
 // the others.
 func registerModuleRecurring(sched *jobs.Scheduler, k *kernel.Kernel, recurring []RecurringJob) {
 	for _, rj := range recurring {
-		rj := rj
 		sched.Register(rj.Name, rj.Every, func(ctx context.Context) error {
 			tenants, err := activeTenants(ctx, k)
 			if err != nil {
