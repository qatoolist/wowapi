---
id: EV-W07-E03-S001-001
type: re-verification-report
parent_story: W07-E03-S001
status: failed
created_at: 2026-07-14
updated_at: 2026-07-14
---

# PROD-01/02/03 direct re-verification

## Evidence metadata

- Evidence ID: `EV-W07-E03-S001-001`
- Evidence type: story re-verification report
- Story and task: `W07-E03-S001`, `W07-E03-S001-T001`
- Acceptance criteria: `AC-W07-E03-S001-01`, `AC-W07-E03-S001-02`, `AC-W07-E03-S001-03`
- Execution date/time: `2026-07-13T21:00:00Z` through `2026-07-13T21:14:39Z`
- Commit SHA: `733ef3e930cbb3f89f5bbc53d8f562c60e426513`
- Branch/tag: `main`
- Environment: local Darwin arm64; PostgreSQL and MinIO from `deployments/compose.yaml`; `DATABASE_URL=postgres://wowapi:REDACTED@localhost:5432/wowapi?sslmode=disable`; `WOWAPI_REQUIRE_DB=1`; `WOWAPI_REQUIRE_S3=1`
- Tool versions: `go version go1.26.5 darwin/arm64`; `psql (PostgreSQL) 18.4`
- Reviewer: `W05ReviewGateFinal` (independent package review `EV-W07-E03-S001-005`)
- Superseded evidence: none
- Retention: repository lifetime
- Status: `failed` because the required PROD-01 parent key is absent; the successful sub-results remain valid

## Expected result

PROD-01 has both the DATA-01 parent key needed by wowsociety's composite FK and the DATA-09 online-migration protocol; PROD-02's FBL-01 shim remains consumable; PROD-03's DX-07/FBL-09 generated readiness and all four server timeouts are present and compile.

## Commands and actual results

### DATA-01 scanner and migration catalog

```text
go test ./internal/tools/tenantfk ./migrations -run 'Test|TestKernelMigrationsHaveManifests' -count=1 -v
```

Result: exit 0. `internal/tools/tenantfk` passed `TestParseMigrationSQLFixture`, `TestScannerEnumerateFixture`, `TestScannerGateNegativeFixture`, and version filtering. `migrations` passed `TestIdentityGrantMigrationUpDown`, `TestIdentityGrantRLSCatalog`, `TestIntegrationMigrationsReversible`, the manifest checks, and migration source checks.

Source inspection confirms `migrations/00034_tenant_fk_parent_indexes.sql` creates the DATA-01 parent-side keys for `parties`, `organizations`, `documents`, and `document_versions`.

Direct live-catalog command:

```text
psql "$DATABASE_URL" -X -v ON_ERROR_STOP=1 -P pager=off -c "SELECT tablename,indexname,indexdef FROM pg_indexes WHERE schemaname='public' AND tablename IN ('rule_versions','identity_grant') ORDER BY tablename,indexname" -c "SELECT column_name,data_type,column_default,is_nullable FROM information_schema.columns WHERE table_schema='public' AND table_name='audit_logs' AND column_name='hash_version'"
```

Relevant actual output for `rule_versions`:

```text
rule_versions | rule_versions_pkey | CREATE UNIQUE INDEX rule_versions_pkey ON public.rule_versions USING btree (id)
rule_versions | rule_versions_lookup | CREATE INDEX rule_versions_lookup ON public.rule_versions USING btree (rule_key, scope_kind, effective_from) WHERE (status = 'active'::text)
rule_versions | rule_versions_rule_key_scope_kind_coalesce_coalesce1_tstzr_excl | CREATE INDEX ... USING gist (...)
```

No unique or exclusion key on `(tenant_id, id)` exists. Actual result: **PROD-01 BLOCKED**. PostgreSQL cannot accept the intended composite product FK until wowapi supplies that parent key.

### DATA-09 protocol

The passing retest is recorded separately as `EV-W07-E03-S001-004`, linked to the resolved infrastructure failure `EV-W07-E03-S001-003`. It passed all current `kernel/migration` protocol tests.

### FBL-01 MFA shim

```text
go test ./kernel/mfa ./foundation/mfa -count=1 -v
```

Result: exit 0. `kernel/mfa` compiled (`[no test files]`). `foundation/mfa` passed the challenge-policy, TOTP, HOTP, sender, boundary, invalid-option, negative-skew, and secret-encoding tests. Source inspection of `kernel/mfa/shim.go` confirms deprecated aliases/forwarders for the canonical `foundation/mfa` surface. Actual result: **PROD-02 READY**, with no exact shim-removal release/owner yet recorded.

### DX-07/FBL-09 generated readiness and timeouts

```text
go test ./app ./internal/cli -run 'TestIntegrationMigrationCurrencyCheck(PassesWhenCurrent|FailsWhenStale)|TestInitAPIMainWiresSeedCatalogsReadinessCheck|TestInitAPIMainConfiguresAllServerTimeouts|TestInitConfigsBaseDocumentsServerTimeouts|TestInitRenderedProductCompiles$' -count=1 -v
```

Result: exit 0. Both migration-currency readiness integration cases passed; the rendered API uses catalog readiness; all four timeout/config tests passed; the rendered product compiled. Actual result: **PROD-03 READY** for a deliberate product backport.

## Conclusion

This evidence fails AC-W07-E03-S001-01 because one of its required framework prerequisites is genuinely absent. It supports the positive PROD-02 and PROD-03 portions without converting the overall story to accepted.
