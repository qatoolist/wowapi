# Clean-line API compatibility allowlist.
#
# v1.2.0 is the bootstrap baseline, so no abandoned-release diagnostics are
# accepted. Add an exact, reviewed diagnostic here only for a supported-line
# release when the compatibility policy explicitly permits it.
