---
id: EV-W00-E01-S002-01
type: evidence
title: PERF-01 re-verification — kernel/httpx race test + bench-budget gate
status: recorded
created_at: 2026-07-13
updated_at: 2026-07-13
derived: false
---

# EV-W00-E01-S002-01 — PERF-01 re-verification — kernel/httpx race test + bench-budget gate

## Evidence ID

EV-W00-E01-S002-01.

## Evidence type

Test-execution log (race-detector run) + benchmark-budget tool output log.

## Story and task

W00-E01-S002 / W00-E01-S002-T001.

## Acceptance criteria proven

AC-W00-E01-S002-01.

## Execution command

1. `go test ./kernel/httpx/... -race`
2. `make bench-budget` (expands to `go test -bench=. -benchmem -run=^$ <BENCH_PKGS> | go run ./internal/tools/benchbudget bench-budgets.txt`)

## Code revision or commit SHA

`0a31186cada5c275a588c74081cf977adf346e61` (which is itself PR #25's merge commit — the SD-03 fix commit).

## Branch or tag

`main`.

## Execution environment

Local development machine — macOS 26.5.2 (Darwin 25.5.0), arm64 (Apple Silicon M3 Max). Machine shared with sibling W00 verification workers running tests concurrently — concurrent load present during timing-sensitive (benchmark) captures.

## Relevant tool versions

go1.26.5 darwin/arm64; GNU Make 3.81; git 2.55.0; benchbudget tool run from source at the same SHA via `go run ./internal/tools/benchbudget`.

## Date and time

2026-07-13, 12:11:56–12:12:14 +05:30 (race test) and 12:12:20–12:13:43 +05:30 (bench-budget).

## Result

**Pass.**
- `go test ./kernel/httpx/... -race` exited **0**; `ok github.com/qatoolist/wowapi/kernel/httpx 3.977s`; no data races reported. Exercises `ratelimit_test.go` (10k-eviction + concurrent Allow/sweep race coverage) with `export_test.go`'s `SweepForTest`/`SeedBucketsForTest` helpers present.
- `make bench-budget` exited **0**; all **43** budgeted benchmarks reported `OK` (no violations), including `BenchmarkTokenBucketSweepAt10k` (451615.0 ns/op, 0 allocs; budget 4500000/0) and `BenchmarkTokenBucketSweepAt100k` (7636231.0 ns/op, 0 allocs; budget 66000000/0).
- Source confirmation of the PERF-01 behavioral claims at this SHA: sweep recomputes refill during the sweep pass itself (`kernel/httpx/ratelimit.go` `sweep()` doc + body, lines ~304-324: "full is computed with the SAME refill formula Allow uses, projected forward from the bucket's own last-touch time"); hard cap enforced deterministically on new-key admission (`WithHardCap`, forced synchronous sweep then rejection, lines ~275-285); backward-compatible 2-arg constructor `NewTokenBucket(ratePerSec float64, burst int)` preserved (line ~228), with options carried by the separate `NewTokenBucketWithOptions` constructor precisely to avoid breaking existing callers.

## File or URI

`artifacts/T001-go-test-kernel-httpx-race.log`; `artifacts/T001-make-bench-budget.log` (story-relative).

## Checksum where appropriate

sha256(T001-go-test-kernel-httpx-race.log) = `7887dcc41ec89ea620bd337d9c662d4c88dda2ee9e4a8757d9227851eb7150fd`
sha256(T001-make-bench-budget.log) = `5fdd45bf8cc951e0e42e5e33c551f0b96145c80fc09baafcd37802c228880745`

## Reviewer

Pending — conductor acceptance gate (worker must not self-accept per W00 status discipline).

## Superseded evidence where applicable

None — first evidence record for this AC.
