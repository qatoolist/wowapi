---
id: EV-W01-E02-S002-001
type: evidence
title: Trace-tree export test — pgx query span as child of parent span (fail-first)
status: recorded
created_at: 2026-07-13
updated_at: 2026-07-13
derived: false
---

# EV-W01-E02-S002-001 — Trace-tree export: pgx child span (fail-first pair)

## Evidence ID

EV-W01-E02-S002-001.

## Evidence type

Integration-test report against a real Postgres, trace tree asserted via the otel adapter's
in-memory span exporter (`tracetest.NewInMemoryExporter`) — parent/child span IDs, not a mock
interaction. Fail-first pair per the task's own script ("confirm no query span exists before
wiring; confirm the query span appears after wiring").

## Story and task

W01-E02-S002 / W01-E02-S002-T001.

## Acceptance criteria proven

AC-W01-E02-S002-01.

## Execution command

`DATABASE_URL='postgres://wowapi:wowapi-local-only@localhost:5432/wowapi?sslmode=disable' go test ./kernel/database/ -run 'TestIntegrationQueryTracer' -v [-race] -count=1`

Run twice: (1) BEFORE — full test file written first, pool fixtures built with plain `NewPool`
(no tracer option; `WithQueryTracer` did not exist yet); (2) AFTER — tracer implemented and the
fixtures' pool construction gains `database.WithQueryTracer(tr)` (the wiring is the only fixture
delta), `-race` enabled.

## Code revision or commit SHA

`0a31186cada5c275a588c74081cf977adf346e61` (HEAD) plus this story's uncommitted working change.

## Branch or tag

`main` (uncommitted working tree).

## Execution environment

Local development machine — macOS (Darwin 25.5.0), arm64 Apple M3 Max; Postgres 16-alpine in
Docker (`wowapi-postgres-1`, compose stack via `make up`), DSN above.

## Relevant tool versions

go1.26.5 darwin/arm64; postgres:16-alpine; pgx v5 (module graph at HEAD).

## Date and time

2026-07-13, ~07:29 UTC (before-run) and ~07:33 UTC (after-run).

## Result

**Pass (fail-first discipline satisfied).**
- Before: `TestIntegrationQueryTracerChildSpanInTraceTree` FAILED — "no query span parented under
  "5ed83fce32cc48bb" in exported trace tree (1 spans exported)" (only the parent span existed).
  Transcript: `ev-001-fail-first-before.txt` (status `failed`, retained).
- After: PASSES — the exported tree contains a `db.SELECT` span whose `Parent.SpanID()` equals the
  parent span's `SpanID()` and whose trace ID equals the parent's trace ID. Additional tree-shape
  proofs in the same run: root span for a parentless query
  (`TestIntegrationQueryTracerRootSpanWithoutParent`) and parent-driven sampling inheritance — an
  unsampled parent exports zero spans (`TestIntegrationQueryTracerInheritsParentSamplingDecision`).
  Transcript: `ev-001-fail-first-after.txt` (status `resolved`).

## File or URI

`tests/ev-001-fail-first-before.txt`; `tests/ev-001-fail-first-after.txt`.

## Checksum where appropriate

sha256(ev-001-fail-first-before.txt) = `3f691dcda86f5bf709f037907b676f8764e7354cd38a6c49dfaa06084195d2a0`
sha256(ev-001-fail-first-after.txt) = `a305275b12c080cce5589e0fb32b8aac79833325a6329757ba7519ad6472f9b1`

## Reviewer

Pending — conductor acceptance gate.

## Superseded evidence where applicable

The before-run (failed) transcript is preserved per the failed-evidence preservation rule.

## Reviewer completion addendum — 2026-07-16

**Reviewer**: Independent review agent (Claude Sonnet 4.5), dispatched 2026-07-16 by Fable 5 conductor (autopsy remediation R-3).
**Review date**: 2026-07-16.
**Commit revision reviewed against**: HEAD 43b6e12 + remediation working tree 2026-07-16.
**Disposition**: Verified. Re-ran the decisive integration test `DATABASE_URL=postgres://wowapi:wowapi-local-only@localhost:5432/wowapi?sslmode=disable WOWAPI_REQUIRE_DB=1 go test ./kernel/database/... -run TestIntegrationQueryTracerChildSpanInTraceTree -v -count=1` against the current tree (DB up): PASS (0.01s). This corrects the autopsy's own noted verification-tooling gap (it used a nonexistent test-name pattern `TestQueryTracer` and got 'no tests to run' without DATABASE_URL set). AC-W01-E02-03 (pgx spans as trace-tree children) stands proven against a real DB, not merely a passing package-level claim.

This addendum retroactively fills the evidence-policy-mandated "reviewer" field. The original
record above (including any "Pending — conductor acceptance gate" line) is left unmodified per
the failed-evidence preservation convention — this is an appended addendum, not a rewrite.
