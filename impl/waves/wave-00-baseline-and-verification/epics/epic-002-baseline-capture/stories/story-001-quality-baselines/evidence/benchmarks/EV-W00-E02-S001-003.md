---
id: EV-W00-E02-S001-003
type: evidence
title: Bench-budget baseline — 43 entries confirmed post-#25, all OK
status: resolved
created_at: 2026-07-13
updated_at: 2026-07-13
derived: false
---

# EV-W00-E02-S001-003 — Bench-budget baseline

## Evidence ID

EV-W00-E02-S001-003

## Evidence type

Benchmark results.

## Story and task

W00-E02-S001 / W00-E02-S001-T003.

## Acceptance criteria proven

AC-W00-E02-S001-03.

## Execution command

Entry-count confirmation: `grep -vc '^#' bench-budgets.txt` → **43**.

Run: `make bench-budget` — which ran, verbatim from `Makefile:223-227`:

```
DATABASE_URL="postgres://wowapi:wowapi-local-only@localhost:5432/wowapi?sslmode=disable" \
  WOWAPI_REQUIRE_DB=1 go test -bench=. -benchmem -run=^$ $(BENCH_PKGS) \
  | go run ./internal/tools/benchbudget bench-budgets.txt
```

`BENCH_PKGS` = the 8 kernel packages (authz, policy, httpx, config, filtering, pagination, audit,
sequence; Makefile:208-217).

## Code revision or commit SHA

`0a31186cada5c275a588c74081cf977adf346e61`

## Branch or tag

`main`

## Execution environment

Local dev workstation — macOS (Darwin 25.5.0), arm64. Real Postgres 16-alpine via compose
(`WOWAPI_REQUIRE_DB=1`). **Concurrent load present** (RISK note): sibling W00 workers were active
on the same machine during the session. Mitigation applied: the timed run was serialized with the
one sibling doing bench work (W00E01S002 confirmed via IRC that all its bench/test runs had
completed before this run started); other siblings' residual non-bench activity cannot be ruled
out, so absolute ns/op figures should be read as a local, load-tolerant snapshot — the
authoritative pass/fail signal is the budget-gate verdict, which passed with headroom.

## Relevant tool versions

- go1.26.5 darwin/arm64
- `internal/tools/benchbudget` at the execution commit
- postgres:16-alpine (compose)

## Date and time

2026-07-13 (run wall time ~80 s).

## Result

**PASS.** `bench-budgets.txt` contains exactly **43** budgeted entries (non-comment lines) —
matching the expected post-#25 (SD-03 recalibration) value per RISK-W00-003; **no drift**.
`make bench-budget` exited 0 with **43/43 benchmarks OK, 0 violations** (grep-confirmed: 43 `OK`
lines, 0 `FAIL`/`OVER` lines in the raw output). This is the post-#25 recalibrated budget state:
the sweep benchmarks (e.g. `BenchmarkTokenBucketSweepAt10k`, 489695.0 ns/op, 0 allocs/op) run
against the recalibrated budgets, not pre-#25 values.

## File or URI

- Raw run output: `../../artifacts/benchmarks/bench-budget-output.txt`
- Entry-count capture: `../../artifacts/benchmarks/bench-budgets-entry-count.txt`
- Authoritative budget file: `bench-budgets.txt` at repository root at the execution commit
  (committed, versioned — referenced, not duplicated).

## Checksum where appropriate

n/a (text log retained in-tree).

## Reviewer

Unassigned (conductor review gate pending).

## Superseded evidence where applicable

None.
