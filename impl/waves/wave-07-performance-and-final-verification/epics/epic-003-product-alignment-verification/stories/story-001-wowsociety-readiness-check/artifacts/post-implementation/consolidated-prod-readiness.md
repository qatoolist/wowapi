---
id: ART-W07-E03-S001-001
type: product-coordination-artifact
parent_story: W07-E03-S001
status: produced
created_at: 2026-07-14
updated_at: 2026-07-14
revision: 733ef3e930cbb3f89f5bbc53d8f562c60e426513
---

# Wowsociety framework-readiness coordination record

## Scope and interpretation

This is a wowapi-only verification at revision `733ef3e930cbb3f89f5bbc53d8f562c60e426513`. No wowsociety repository was read or changed. A framework capability is marked ready only where the current wowapi source, generated template, migration catalog, live migrated PostgreSQL catalog, and focused tests agree. A product-side action remains for every row; those actions are coordination guidance, not claims about wowsociety's present code.

## Consolidated disposition

| Item | Framework status | Direct framework evidence | Genuine gap | Owner / coordination path | Concrete product upgrade path |
|---|---|---|---|---|---|
| PROD-01 — DATA-01/DATA-09 policy override FK | **BLOCKED** | DATA-01 parent indexes exist for `parties`, `organizations`, `documents`, and `document_versions` in `migrations/00034_tenant_fk_parent_indexes.sql`; the scanner's positive and negative fixtures pass. DATA-09's expand/backfill/validate/canary/switch/contract primitives and manifest enforcement pass in `kernel/migration`. The live catalog nevertheless has only `rule_versions_pkey(id)`, a partial lookup index, and an exclusion constraint: the direct `pg_indexes` probe found **no** `UNIQUE (tenant_id, id)` key. | The composite product FK `(tenant_id, rule_version_id) -> rule_versions(tenant_id, id)` cannot be created because PostgreSQL requires a matching unique/exclusion key on the referenced columns. This is the exact DATA-01 parent-key prerequisite, not a product-side workaround opportunity. | wowapi data/reliability owner first: add and verify an online, manifest-classified `UNIQUE (tenant_id, id)` parent key. Then wowsociety schema owner coordinates the product migration using DATA-09. | Do not start the FK cutover yet. After a released wowapi migration supplies the parent key: (1) inventory/null and tenant-mismatch audit `policy_override` rows; (2) add the composite FK `NOT VALID` while keeping the old FK/read path; (3) backfill/repair under a resumable checkpoint; (4) validate in a bounded phase; (5) canary and switch reads/writes; (6) contract the old single-column FK only after rollback criteria pass. |
| PROD-02 — FBL-01 MFA re-home | **READY**, with a scheduling gap | `kernel/mfa/shim.go` is explicitly a deprecated forwarding shim to `foundation/mfa`. It preserves constants, aliases for `ChallengeState`, `ChallengePolicy`, `TOTPOptions`, `Sender`, `FakeSender`, and `Algorithm`, plus forwarding functions for secret encoding/decoding, TOTP/HOTP and challenge-policy behavior. `go test ./kernel/mfa ./foundation/mfa -count=1 -v` passed; the shim package compiles and the canonical package's behavior tests pass. | The shim comment promises removal in a future minor version, but no exact removal release or named coordination owner is recorded. This does not block migration now, but it prevents a precise sunset deadline. Product call sites and product tests were not inspected by this framework-only story. | wowsociety identity owner replaces imports; wowapi DX/FBL-01 owner must publish the exact shim-removal release and coordinate it through the normal dependency-upgrade channel. | Replace each `github.com/qatoolist/wowapi/kernel/mfa` import with `github.com/qatoolist/wowapi/foundation/mfa`; no symbol rename should be needed because the shim aliases/forwards the API. Format the five affected identity files, run their focused MFA/authentication tests, then remove the deprecated import before upgrading to the announced removal release. Do not copy the shim into product code. |
| PROD-03 — DX-07/FBL-09 scaffold readiness and timeouts | **READY** | The generated API uses `app.ReadinessWithCatalogs`, exposing `migration_currency` and `seed_catalogs`; the current/stale DB readiness integration tests pass. The API template assigns all four connection-level server budgets from config: `ReadHeaderTimeout`, `ReadTimeout`, `WriteTimeout`, and `IdleTimeout`. The base template documents `10s`, `30s`, `60s`, and `120s`; the scaffold tests and a rendered-product compile test pass. | wowsociety's committed `main.go` is outside this story and was not inspected. A committed product is not automatically updated when templates change; it needs an explicit backport. | wowsociety application/bootstrap owner performs the backport; wowapi DX owner supplies the generated template as the reference and reviews deviations. | Diff a fresh `wowapi init` rendering against the committed product bootstrap. Port `app.ReadinessWithCatalogs` with the product's booted app, fingerprint, database pool, kernel migrations/source, and seed catalogs so stale migrations or catalogs make `/readyz` fail closed. Add config keys `read_header_timeout: 10s`, `read_timeout: 30s`, `write_timeout: 60s`, and `idle_timeout: 120s`, wire all four into `http.Server`, then run product readiness integration tests and a slow-header/body/write/idle timeout smoke test. |
| PROD-04 — SEC-01 grant contract and rollout | **BLOCKED for product cutover** (framework contract ready; rollout artifact stale) | Live migration `00039_identity_grant.sql` creates the tenant-scoped FORCE-RLS grant source of truth, a partial one-active-grant-per-actor index, and least-privilege grants. Live catalog probes confirm the columns, RLS policies, platform/app grants, and active-grant index. `PrincipalStore.ResolveGrant` validates tenant, actor, status, expiry, revocation, target and a distinct active tenant approver. `Verifier.Actor` ignores direct impersonation/break-glass claims and derives them only from a verified `grant_id`. Resolver, rejection, and concurrency tests pass. | W03-E01-S004's rollout material is not current enough to execute safely: `sequencing-plan.md` locates the migration under `adapters/auth/pgprincipal/migrations` instead of root `migrations/00039_identity_grant.sql` and claims legacy direct claims still populate `Actor`; current `Verifier.Actor` explicitly ignores them. `staging-validation-plan.md` queries nonexistent `actor_user_id`, `impersonator_user_id`, and `break_glass` grant columns. `rollback-plan.md` proposes returning to direct-claim trust/fallback even though the current framework rejects that authority path. The required wowsociety reviewer/sign-off is also still `TBD`. | wowsociety identity owner and wowapi SEC-01 owner jointly rewrite/revalidate W03-E01-S004 against the current released contract; product-security signs off before any dependency bump or token change. | Block the framework upgrade until the product can mint a real grant and put its ID in the supported `grant_id` claim. For impersonation, insert an active tenant grant whose `actor_id` is the operator, `impersonated_user_id` is the target, `approver_id` is distinct and actively entitled in the tenant, and whose expiry/revocation fields satisfy policy. Update token minting, whoami/audit presentation, and tests together; direct `impersonator`/`break_glass` claims are not a compatibility bridge. Rehearse old-token behavior explicitly. Roll back by reverting the product/framework version pair or disabling privileged issuance—not by restoring unverified direct claims. Replace the stale SQL/checklists with the real schema and obtain both wowapi and wowsociety reviewer signatures. |
| PROD-05 — D-04 `hash_version` verification | **READY for a product staging drill** | Migration `00037_audit_hash_version.sql` adds non-null `hash_version` default `1` with an online manifest. Audit writes use version `2`; verifier dispatch supports historical v1 and current v2, and unknown versions fail closed. Focused DB integration proves v1/v2 coexistence, unknown-version rejection, and per-field tamper detection across the widened v2 payload; migration-manifest validation passes. | No framework capability gap was found. The required all-chain staging result is necessarily product/environment evidence and was not performed by this framework-only story. | wowsociety compliance/data owner runs and archives the staging drill; wowapi audit owner reviews any failure before the product upgrades. | Apply the released migration and audit code in staging without rewriting historical rows. Generate at least one new audit event and confirm it is `hash_version=2`; retain v1 rows. Run the framework verifier over every tenant/chain and require zero sequence, predecessor, hash, or unknown-version failures. Exercise the same operational verification command used for DATA-08, record row/version counts and exact failing IDs if any, and only then advance the dependency. Any unknown version or broken chain blocks rollout and is investigated—never normalized in place. |

PROD-05 command specificity: enumerate the staging tenant UUIDs, then run
`wowapi audit verify --tenant <uuid>` under the product runtime DSN for every tenant. Exit `0` is
intact; any nonzero result blocks the version bump and must be archived with the affected tenant and
chain details. Pair this with version counts and a new v2 write as described in the row above.

## Verification summary

Environment: `go1.26.5 darwin/arm64`, PostgreSQL client 18.4, PostgreSQL/MinIO services from `deployments/compose.yaml`, `DATABASE_URL=postgres://wowapi:REDACTED@localhost:5432/wowapi?sslmode=disable`, `WOWAPI_REQUIRE_DB=1`, `WOWAPI_REQUIRE_S3=1`.

Focused results at the pinned revision:

- DATA-09 protocol: `go test ./kernel/migration -count=1 -v` — PASS after the required local PostgreSQL service was started; expand/backfill/validate/canary/switch/contract and manifest tests passed.
- DATA-01 scanner and migration catalog: `go test ./internal/tools/tenantfk ./migrations -run 'Test|TestKernelMigrationsHaveManifests' -count=1 -v` — PASS, including scanner fixtures, identity-grant migration tests, manifest coverage, and migration reversibility.
- FBL-01 shim/canonical behavior: `go test ./kernel/mfa ./foundation/mfa -count=1 -v` — PASS (`kernel/mfa` compiles; `foundation/mfa` behavior suite passes).
- DX-07/FBL-09: `go test ./app ./internal/cli -run 'TestIntegrationMigrationCurrencyCheck(PassesWhenCurrent|FailsWhenStale)|TestInitAPIMainWiresSeedCatalogsReadinessCheck|TestInitAPIMainConfiguresAllServerTimeouts|TestInitConfigsBaseDocumentsServerTimeouts|TestInitRenderedProductCompiles$' -count=1 -v` — PASS.
- SEC-01: `go test ./kernel/auth ./adapters/auth/pgprincipal -run 'TestActor_(PrivilegedSessionResolvedFromGrant|DirectImpersonationClaimIgnoredWithoutGrantID|ForgedGrantIDRejected|ExpiredGrantRejected|RevokedGrantRejected|WrongTenantGrantRejected|WrongActorGrantRejected|UnauthorizedApproverGrantRejected)|TestIdentityGrantOneActivePerActorConcurrent|TestResolveGrant_' -count=1 -v` — PASS.
- D-04: `go test ./kernel/audit ./migrations -run 'TestIntegrationAudit(HashVersionBranching|UnknownHashVersionFailsClosed|ChainDetectsPerFieldTampering)|TestKernelMigrationsHaveManifests' -count=1 -v` — PASS.
- D-04 staging command: `go test ./internal/cli -run 'TestAudit(NoArgsUsage|UnknownSubcommand|BadTenant|MissingDSN|VerifyEmptyTenantDB|VerifyIntactAfterWriteDB|VerifyDetectsTamperDB)' -count=1 -v` — PASS, proving `wowapi audit verify --tenant <uuid>` intact/tamper and error/exit paths.
- Direct PostgreSQL catalog probes — PASS as probes, with a blocking negative finding for PROD-01: `rule_versions` has no `UNIQUE (tenant_id,id)`; SEC-01 schema/RLS/grants/indexes and the `audit_logs.hash_version` column match the source migrations.

Exact commands, timestamps, revision, results, and the initial resolved infrastructure failure are registered in `evidence/index.md` and its linked records.

## Coordination decision

The verification package is complete, but W07-E03-S001 cannot be accepted: AC-W07-E03-S001-01 fails on PROD-01, and AC-W07-E03-S001-04 fails on PROD-04's stale coordinated rollout artifact. PROD-02, PROD-03, and PROD-05 are ready for their documented consumer-side actions. No wowsociety change is made or implied by this record.
