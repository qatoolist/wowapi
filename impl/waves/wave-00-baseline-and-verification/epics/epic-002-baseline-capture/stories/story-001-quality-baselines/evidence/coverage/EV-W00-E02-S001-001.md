---
id: EV-W00-E02-S001-001
type: evidence
title: Coverage baseline — 92.3% vs 90.0% floor (pass)
status: resolved
created_at: 2026-07-13
updated_at: 2026-07-13
derived: false
---

# EV-W00-E02-S001-001 — Coverage baseline

## Evidence ID

EV-W00-E02-S001-001

## Evidence type

Coverage report.

## Story and task

W00-E02-S001 / W00-E02-S001-T001.

## Acceptance criteria proven

AC-W00-E02-S001-01.

## Execution command

`make coverage-check` — which ran, verbatim from `Makefile:236-246`:

```
DATABASE_URL="postgres://wowapi:wowapi-local-only@localhost:5432/wowapi?sslmode=disable" \
  WOWAPI_REQUIRE_DB=1 go test -coverprofile=coverage.out $(COVER_PKGS)
go tool cover -func=coverage.out | tail -1
go tool cover -html=coverage.out -o coverage.html
# floor comparison: awk against COVERAGE_FLOOR=90.0
```

`COVER_PKGS` = `go list ./...` minus `/cmd/wowapi|/internal/tools/migrate|/internal/testmodules|/module$` (Makefile:233-234).

## Code revision or commit SHA

`0a31186cada5c275a588c74081cf977adf346e61`

## Branch or tag

`main`

## Execution environment

Local dev workstation — macOS (Darwin 25.5.0 / product 26.5.2), arm64. Real Postgres 16-alpine +
MinIO via `deployments/compose.yaml` (containers up and healthy; `WOWAPI_REQUIRE_DB=1`, per-test
databases cloned from a migrated template per `testkit/db.go`). **Concurrent load present**:
sibling W00 verification workers were running test suites on the same machine and Postgres
instance during this capture (per-test DB cloning isolates correctness; only wall-clock timing of
the run itself was affected, not the coverage percentage, which is deterministic).

## Relevant tool versions

- go1.26.5 darwin/arm64
- postgres:16-alpine (compose)

## Date and time

2026-07-13 (run wall time ~59 s).

## Result

**PASS.** `total: (statements) 92.3%` — floor 90.0%, exit code 0. Every measured package passed
its tests; no package below its own expectations. Full per-package breakdown in the artifact.

Observation required by T001 step 6: the fresh 92.3% is consistent with (not materially different
from) the prior ~92% figure recorded in project history; that prior figure is hereby reconfirmed
as ~92.3% at this commit, measured fresh — this record, not the prior history, is the citable
baseline.

## File or URI

- Raw run output: `../../artifacts/coverage/coverage-check-output.txt` (this story tree)
- Generated profile/report: `coverage.out` / `coverage.html` at repository root at the execution
  commit (build artifacts, untracked/ignored — referenced by generation command per the
  no-duplication rule, not copied into `impl/`).

## Checksum where appropriate

n/a (text log retained in-tree).

## Reviewer

Unassigned (conductor review gate pending).

## Superseded evidence where applicable

None.
