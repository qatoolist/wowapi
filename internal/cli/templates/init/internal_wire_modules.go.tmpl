// Package wire is the product-specific module registry maintained by
// `wowapi new-module`.
package wire

import (
	"github.com/qatoolist/wowapi/v2/module"
	// wowapi:module-imports
)

// Modules returns the product-registered module set. Registration order does
// not matter: the framework derives actual startup order from each module's
// DependsOn graph.
func Modules() []module.Module {
	return []module.Module{
		// wowapi:module-values
	}
}
