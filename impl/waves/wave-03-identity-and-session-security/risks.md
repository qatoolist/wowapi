---
id: W03-RISKS
type: wave-risks
wave: W03
status: planned
created_at: 2026-07-12
updated_at: 2026-07-12
---

# W03 — Risks

| Risk ID | Description | Likelihood | Impact | Severity | Affected items | Mitigation | Contingency | Owner | Status | Residual risk |
|---|---|---|---|---|---|---|---|---|---|---|
| RISK-W03-001 | DEC-Q1 (IdP `grant_id` claim contract) remains unresolved through this wave's execution, forcing rework of W03-E01-S001's grant-ID claim-shape assumption once the human decision finally lands | Medium | Medium — the safe default (framework owns the grant record keyed by grant-ID, IdP claim shape is tuning) is designed to absorb this, but a sufficiently different final claim shape could still require resolver changes in W03-E01-S002 | Medium | W03-E01-S001, W03-E01-S002 | Build strictly against the documented safe default (REVIEW §F row 1 / MATRIX CS-07): the framework owns the grant record and looks it up by session/grant-ID; the JWT only needs to carry a stable subject. Do not pre-guess a specific claim shape beyond what the safe default requires. | If DEC-Q1 resolves to a materially different claim shape after E01-S001/S002 are accepted, record a deviation and schedule a follow-up story rather than reopening the accepted stories' scope silently | unassigned | open | Low — the safe default is explicitly designed to make this a tuning change, not a redesign |
| RISK-W03-002 | The wowsociety two-repo coordinated cutover for SEC-01's impersonation-flow breaking change (PROD-04) cannot be completed unilaterally by this wave — wowapi can ship T1+T5, but the cutover itself requires wowsociety's own `startImpersonation`/`stopImpersonation` rework and staging validation, which is outside this programme's framework-implementation scope | High (structural, not a defect) | High — an uncoordinated cutover risks breaking wowsociety's live impersonation flow, or (if deferred indefinitely) leaves the framework's new server-side grant authority unused by its only current consumer | High | W03-E01-S004 | E01-S004 produces the sequencing/staging-validation/rollback coordination plan (PROD-04) as a documentation artifact — this wave's scope is the plan, not the cutover execution. The plan explicitly states the two-repo coordination requirement rather than assuming a single-repo rollout. | If wowsociety-side coordination cannot begin promptly after this wave closes, record the gap in `deferred-items-register.md` at programme-tracking scope (outside this wave's own files) with the risk of prolonged JWT-trust exposure noted | unassigned | open | Medium — accepted as a structural cross-repo risk this wave's documentation-only scope cannot itself close |
| RISK-W03-003 | DATA-07 T4's cache-invalidation acceptance criterion depends on W05-E04-S002 (SEC-04 epoch table, D-06) landing — if W05 is delayed relative to W03, E04-S001 either ships with an incomplete AC or must artificially wait on a later wave | Medium | Medium — PLAN's own cross-cutting note warns explicitly: "do not assume PF-SEC delivers on PF-DATA's timeline," which at this wave's scope inverts to "do not assume W05 delivers the epoch table on W03's timeline" | Medium | W03-E04-S001 | E04-S001's T4 acceptance criterion is deferred-linked to W05-E04-S002 rather than blocking E04's overall acceptance — T1/T2/T4's non-cache-invalidation portions can close independently. This is recorded explicitly in `dependencies.md`, not silently assumed. | If W05-E04-S002 has not landed by the time E04-S001 is otherwise ready to close, close E04 with the cache-invalidation AC explicitly marked deferred-linked (not silently dropped) and track it in the technical-debt or deferred-items register at programme scope | unassigned | open | Low-medium once the deferred-link framing is honored |
| RISK-W03-004 | SEC-01 T2's "every existing valid session must have a live `user_tenant_access` row" precondition may not hold against real production/staging data — PLAN's own risk column: "audit production data first" | Medium | High — if unconditional membership enforcement is flipped on against data with gaps, currently-working sessions would be incorrectly rejected | High | W03-E01-S001 | E01-S001's task breakdown includes a data-audit step against `user_tenant_access` before T2's unconditional-check behavior is enabled, per PLAN's own risk note; the wowsociety staging-validation leg of E01-S004 explicitly covers this for the product's own data | Roll out T2 behind a profile flag or staged enforcement if the audit finds gaps, rather than flipping unconditional enforcement on blind | unassigned | open | Medium — genuinely data-dependent, cannot be fully retired by design alone |
| RISK-W03-005 | SEC-01 T4's server-side capacity-selection requirement ("Require explicit capacity choice when >1 active") breaks any currently-working capacity-less multi-capacity flow, per PLAN's own risk column: "needs a product-side UX" | Medium | Medium — a currently-functioning (if under-verified) product flow could be broken without a coordinated product-side UX change | Medium | W03-E01-S002 | E01-S002's story records this as a compatibility consideration requiring product-side coordination, tracked alongside E01-S004's cutover plan rather than assumed away | Flag to product/wowsociety as part of E01-S004's coordination scope if a capacity-less multi-capacity flow is found to be in active use | unassigned | open | Medium — depends on product-side UX timeline, outside this wave's unilateral control |
| RISK-W03-006 | SEC-03's breaking `Verifier` interface change (T1) has zero current wowsociety consumers per `requirement-inventory.md`/PLAN's own wowsociety-impact note ("Zero `kernel/webhook` import, zero custom `Verifier` implementation anywhere in wowsociety"), but a future or undiscovered custom `Verifier` implementation would break silently at compile time only, not runtime | Low | Low — a compile break is a safe failure mode, not a silent behavioral regression | Low | W03-E03-S001 | Re-confirm the zero-consumer claim at this story's own execution time (fresh grep), not merely trust the cited snapshot, consistent with wave-01's pattern of re-confirming cited evidence before acting on it | If a consumer is found, document it in `deviations.md` and coordinate the interface change with that consumer before merge | unassigned | open | Low |
