# Progress

| Date | Phase | Event |
|---|---|---|
| 2026-07-03 | 0 | Preflight: 5 blueprint inconsistencies fixed (D-0001…D-0005); planning artifacts created (phase-plan, risk-register, test-strategy, decisions, readiness-checklist, evidence structure). |
| 2026-07-03 | 0 | Walking skeleton: go.mod, kernel/secrets, kernel/config (Env/Secret/Framework/ModuleView), module + app skeletons, internal/buildinfo + internal/cli, cmd/wowapi version, boundary lint script, Makefile, Dockerfile, compose stack, unit tests. |
| 2026-07-03 | 0 | Verification + review pass recorded in evidence/phase-00/; Phase 0 committed. |
| 2026-07-03 | 1 | Loader core (TDD): layered precedence, strict binder (D-0012), fail-closed environment (D-0010), unsafe-knob matrix (D-0015), secrets resolution/redaction, Namespaces, fingerprint, schema. yaml.v3 added (D-0011). |
| 2026-07-03 | 1 | 3 parallel agents delivered kernel/logging, app views/context/RunHooks + adapters/secrets/envprovider (D-0013), CLI config validate/print/schema/doctor. Docker gates run: `make up` + `make ci-container` green (after dev-image cgo fix) — **R4 closed**. |
| 2026-07-03 | 1 | Review pass: security agent (SEC-3…SEC-10, three reproduced fail-open bugs) + architecture agent (ARCH-6…ARCH-15). All findings fixed with regression tests or accepted with rationale (D-0016…D-0019). Host+container CI green; Phase 1 committed (`001c4c1`). |
| 2026-07-03 | 2 | kernel/database (pool, TxManager/TenantDB, RLS, migrate runner), kernel/model, migrations 000–001, tenant/user/access tables, testkit + AssertRLSIsolation. 3 parallel agents (model/migrations/testkit) + lead. Deps: pgx, uuid, goose, decimal (D-0020…D-0025). |
| 2026-07-03 | 2 | R1 security review REPRODUCED two critical isolation holes (SEC-11 role escalation, SEC-12 RLS-off-by-misconfig) + SEC-13 global-table grants; architecture review (ARCH-16…ARCH-26). Fixed: non-superuser runtime login + connect/per-tx RLS guards, app_platform grants, per-source migrations (D-0023 rev, D-0026…D-0030). Live idempotency + RLS isolation green host + container; Phase 2 committed (`4a4b3f3`). |
| 2026-07-03 | 3 | kernel/errors (closed taxonomy), kernel/httpx (RouteMeta enforcement, problem-details, strict decode, middleware, idempotency), kernel/validation, kernel/pagination, kernel/filtering (+KeysetClause), idempotency store + migration 00003. 2 parallel agents + lead (D-0031…D-0034). |
| 2026-07-03 | 3 | Security + architecture reviews both REPRODUCED the idempotency concurrency race (SEC-16/ARCH-27: double-execute + clobbered response); fixed with atomic ON-CONFLICT-DO-NOTHING claim + 8-goroutine regression (×5 -race). Also SEC-18 recover-corruption, ARCH-29 null-body, ARCH-31 KeysetClause, ARCH-30 depguard. Host+container CI + integration green; Phase 3 committed (`167eee9`). |
| 2026-07-03 | 4 | Identity/authz: kernel/auth (OIDC verifier, alg-confusion defended), kernel/authz (deny-by-default RBAC/ReBAC/ABAC evaluator + pg Store), kernel/policy, kernel/relationship, kernel/resource; migrations 00004–00006. 2 parallel agents + lead (D-0035…D-0037). |
| 2026-07-03 | 4 | Security review REPRODUCED live: ReBAC self-grant (SEC-24), ABAC deny fail-open (SEC-25), resource_type over-grant (SEC-26); architecture found evaluator opened per-check txns (ARCH-36). Fixed: authz spine SELECT-only to app_rt (RBAC+ReBAC self-grant closed, live-verified), deny fail-closed, scope CHECKs, evaluator threads request TenantDB (D-0038/D-0039). Host+container CI + integration green; Phase 4 committed (`03ce981`). |
| 2026-07-03 | 5 | Module SDK + Kernel/App boot (wires evaluator, gates permission registry — closes Phase 4 deferral), seed loader, neutral fixture module, testkit contract suite + fixtures, external scratch-consumer test. 1 agent + lead (D-0040…D-0043). |
| 2026-07-03 | 5 | Review REPRODUCED seed-ownership holes (SEC-32 role self-grant, SEC-34 granted_via) + SEC-33 (seeds never tested under app_platform). Fixed: seed grant/granted_via ownership + reconciliation, app_platform seed path with app_tenant_id_or_null hybrid-table RLS, diff-based contract RLS check (D-0044…D-0046). Contract + scratch-consumer green host+container; Phase 5 committed (`1985610`). |
| 2026-07-03 | 6 | Transactional outbox + relay/inbox, Postgres job runner (retries/DLQ, D-0047), worker process; migration 00007. 1 agent (jobs) + lead (D-0047…D-0049). |
| 2026-07-03 | 6 | Review REPRODUCED per-aggregate ordering NOT held (ARCH-53, advisory lock absent) + event-DLQ/cooldown/job-timeout/shutdown gaps. Fixed: earliest-per-aggregate claim + advisory lock, event DLQ, jobTimeout≠drain, hard shutdown cap, reclaim floor (D-0050). Atomicity + tenant isolation verified solid. Host+container CI + integration green; Phase 6 committed. |
| 2026-07-03 | 7 | Rules engine (registry, versioned rows, temporal resolution, draft/activate approval gating) + workflow engine (closed step set, boot validation, optimistic-lock runtime, same-tx outbox); migrations 00008/00009. 1 agent (workflow) + lead (rules, migrations, wiring, D-0051…D-0053). |
| 2026-07-03 | 7 | Review (2 agents: security+arch) REPRODUCED 8 gaps. Fixed: historical resolution incl. superseded (ARCH-60), write-time schema validation (SEC-40), draft/activate privilege split + created_by (SEC-13/ARCH-62), workflow fail-closed on unenforced vote/min_approvals/self_approval + Override authz gate (SEC-36/37/38/39), approval-completeness (ARCH-64) — D-0054. Also root-caused a container-only flaky (TestVerify_TamperedSignature, base64 padding-bit flip; 200× stable). Host `make ci`+integration and `make ci-container` green; Phase 7 committed. |
| 2026-07-04 | 8 | Document/file framework: storage Adapter port + memory adapter, kernel/document (class registry, presigned upload sessions w/ size+checksum+MIME-sniff verification, append-only versions, deny-first+owner+grant download, scan gate, retention sweep + legal hold), kernel/comment (threaded, edit-history CAS), kernel/attachment; migration 00010 (+app_actor_id()). 1 agent (comment/attachment) + lead (storage/document/migration/wiring, D-0055). |
| 2026-07-04 | 8 | Review (2 agents: security+arch) REPRODUCED 13 gaps. Fixed: grant self-grant/escalation via RLS ownership + app_actor_id() (SEC-41/42), unguarded revoke (SEC-43), legal_hold/status column-lockdown (SEC-44), comment/attachment author guards (SEC-45/46), pre-authz version leak + read-only-tx break (SEC-47/ARCH-65), retention delete-ordering (SEC-48), upload-key collision (ARCH-66), error-masking (ARCH-67), voided-version attach (ARCH-68), MIME essence (ARCH-69) — D-0055. Host `make ci`+integration + `go test -p 1` in-container green; noted a pre-existing testkit template/role-provisioning race (warm-template parallel container CI green). Phase 8 committed. |
| 2026-07-04 | 9 | Notify/webhook/integration framework: kernel/notify (template registry, Send-in-tx, async SendPending w/ backoff+DLQ), kernel/webhook (inbound verify/replay/window, ProcessInbound + RetryOutbound, HMAC outbound, per-endpoint circuit breaker), kernel/integration (provider registry, config + secret-ref credentials, health); migration 00011 (+events_outbox app_platform INSERT). 2 agents (notify; webhook) + lead (integration, migration, wiring, D-0056). |
| 2026-07-04 | 9 | Review (2 agents: security+arch) REPRODUCED 13 gaps (1 critical, 1 high). Fixed: outbound-never-retried (ARCH-70 RetryOutbound), phantom Upsert id (ARCH-71), degraded-never-cleared (ARCH-72), missing backoff/index (ARCH-73/75), incomplete-vars (ARCH-77), NULL-id replay (SEC-49), failed-sig audit DoS (SEC-50), email XSS via text/template (SEC-51), unsigned timestamp (SEC-52), hybrid-RLS platform-write (SEC-53) — D-0056; fixes driven back through the authoring agents. Host `make ci` + `go test -p 1` and `make ci-container` green. Phase 9 committed. |
