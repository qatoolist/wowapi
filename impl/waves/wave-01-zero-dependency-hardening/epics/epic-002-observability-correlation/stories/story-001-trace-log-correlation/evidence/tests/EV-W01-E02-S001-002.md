---
id: EV-W01-E02-S001-002
type: evidence
title: Negative-case test — correlation keys genuinely absent without a span
status: recorded
created_at: 2026-07-13
updated_at: 2026-07-13
derived: false
---

# EV-W01-E02-S001-002 — Negative case: keys genuinely absent (not empty-string)

## Evidence ID

EV-W01-E02-S001-002.

## Evidence type

Unit-test report (key-ABSENCE assertion, per RISK-W01-E02-002's explicit test-design requirement).

## Story and task

W01-E02-S001 / W01-E02-S001-T002.

## Acceptance criteria proven

AC-W01-E02-S001-02.

## Execution command

1. `go test ./kernel/logging/ -run 'TestLogRecordWithoutSpanOmitsCorrelationKeys' -v` —
   end-to-end through `logging.New`, both flavors: `context.Background()` (no span) and the
   `NoOpTracer.StartSpan` context. Asserts the decoded JSON record does NOT CONTAIN the
   `trace_id`/`span_id` keys (`if v, ok := rec["trace_id"]; ok { fail }`), not that values are `""`.
2. `go test ./kernel/observability/ -run 'TestCorrelatingHandlerMatrix' -v -race` — the wrapper-level
   contract matrix, including the fourth row "span present but TraceID empty → keys absent"
   (guards the empty-string-noise path directly at the injection site).

## Code revision or commit SHA

`0a31186cada5c275a588c74081cf977adf346e61` (HEAD) plus this story's uncommitted working change.

## Branch or tag

`main` (uncommitted working tree).

## Execution environment

Local development machine — macOS (Darwin 25.5.0), arm64 Apple M3 Max.

## Relevant tool versions

go1.26.5 darwin/arm64.

## Date and time

2026-07-13, ~07:26–07:35 UTC (also re-run inside the touched-package `-race` regression sweep).

## Result

**Pass.** `TestLogRecordWithoutSpanOmitsCorrelationKeys` (both subtests) and
`TestCorrelatingHandlerMatrix` (all four rows) pass; the negative rows assert key absence from the
decoded attribute set. Notably the negative subtests already passed in the BEFORE run of
EV-W01-E02-S001-001 (`ev-001-fail-first-before.txt`) — i.e. absence was never violated at any point,
including pre-fix.

## File or URI

`tests/ev-001-fail-first-after.txt` (contains the kernel/logging negative subtests);
`regression/touched-packages-race.txt` (full `-race` sweep containing both packages).

## Checksum where appropriate

sha256(ev-001-fail-first-after.txt) = `f31520c6cf4d6140027f9ecc1937ea125bd0390f631f9ae01a1ada1ad3132c55`
sha256(regression/touched-packages-race.txt) = `c00a26ee4f240f63636f80a64a992b95513c5a114a92ddb7a3d19a056fffe903`

## Reviewer

Pending — conductor acceptance gate. Independent review must specifically confirm the key-absence
(not empty-string) assertion shape per the story's Definition of done.

## Superseded evidence where applicable

None — first evidence record for this AC.
