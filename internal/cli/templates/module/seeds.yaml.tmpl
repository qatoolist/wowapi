# Seed catalog for the {{.Name}} module.
# All permission/resource/role keys must be prefixed "{{.Name}}.".
# Field names match seeds.Bundle YAML tags exactly (KnownFields strict decode).

permissions: []

resource_types: []

relationship_types: []

roles: []
