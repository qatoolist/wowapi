---
id: EV-W07-E03-S001-002
type: re-verification-report
parent_story: W07-E03-S001
status: failed
created_at: 2026-07-14
updated_at: 2026-07-14
---

# PROD-04/05 direct re-verification

## Evidence metadata

- Evidence ID: `EV-W07-E03-S001-002`
- Evidence type: story re-verification report
- Story and task: `W07-E03-S001`, `W07-E03-S001-T002`
- Acceptance criteria: `AC-W07-E03-S001-02`, `AC-W07-E03-S001-04`, `AC-W07-E03-S001-05`
- Execution date/time: `2026-07-13T21:00:00Z` through `2026-07-13T21:14:39Z`
- Commit SHA: `733ef3e930cbb3f89f5bbc53d8f562c60e426513`
- Branch/tag: `main`
- Environment: local Darwin arm64; PostgreSQL and MinIO from `deployments/compose.yaml`; `DATABASE_URL=postgres://wowapi:REDACTED@localhost:5432/wowapi?sslmode=disable`; `WOWAPI_REQUIRE_DB=1`; `WOWAPI_REQUIRE_S3=1`
- Tool versions: `go version go1.26.5 darwin/arm64`; `psql (PostgreSQL) 18.4`
- Reviewer: `W05ReviewGateFinal` (independent package review `EV-W07-E03-S001-005`)
- Superseded evidence: none
- Retention: repository lifetime
- Status: `failed` because the required PROD-04 coordinated rollout artifact contradicts the current framework; the passing SEC-01 code and PROD-05 results remain valid

## Expected result

PROD-04 has both a verified SEC-01 grant contract and a current, executable, signed-off product rollout artifact. PROD-05 has a verified D-04 hash-version branch suitable for a product staging drill.

## Commands and actual results

### SEC-01 grant contract

```text
go test ./kernel/auth ./adapters/auth/pgprincipal -run 'TestActor_(PrivilegedSessionResolvedFromGrant|DirectImpersonationClaimIgnoredWithoutGrantID|ForgedGrantIDRejected|ExpiredGrantRejected|RevokedGrantRejected|WrongTenantGrantRejected|WrongActorGrantRejected|UnauthorizedApproverGrantRejected)|TestIdentityGrantOneActivePerActorConcurrent|TestResolveGrant_' -count=1 -v
```

Result: exit 0. All selected `Verifier.Actor`, grant concurrency, impersonation/break-glass success, and rejection-path tests passed.

Live-catalog commands:

```text
psql "$DATABASE_URL" -X -v ON_ERROR_STOP=1 -P pager=off -c "SELECT column_name,data_type,is_nullable FROM information_schema.columns WHERE table_schema='public' AND table_name='identity_grant' ORDER BY ordinal_position" -c "SELECT grantee,privilege_type FROM information_schema.role_table_grants WHERE table_schema='public' AND table_name='identity_grant' ORDER BY grantee,privilege_type" -c "SELECT polname,polcmd,qual,with_check FROM pg_policies WHERE schemaname='public' AND tablename='identity_grant' ORDER BY polname"
```

Result: exit 0. Actual schema includes `id`, `status`, `tenant_id`, `actor_id`, `impersonated_user_id`, `approver_id`, `reason`, `activated_at`, `expires_at`, and `revoked_at`. RLS is forced by source migration; `identity_grant_tenant_isolation` uses/checks `tenant_id = app_tenant_id()`; platform policy is unrestricted; `app_platform` has `SELECT`, `INSERT`, and `UPDATE` only, while `app_rt` has no grant-table privileges. The active-grant partial unique index exists on `actor_id WHERE status='active'`.

Source inspection of `kernel/auth/auth.go` and `adapters/auth/pgprincipal/pgprincipal.go` confirms direct privileged claims are ignored and grant-derived actor state validates tenant, actor, status, expiry, revocation, impersonation target, and a distinct actively entitled approver.

### SEC-01 rollout artifact cross-check

The required artifact is W03-E01-S004's `sequencing-plan.md`, `staging-validation-plan.md`, and `rollback-plan.md`. Direct comparison found blocking contradictions:

1. `sequencing-plan.md` points to `adapters/auth/pgprincipal/migrations/00039_identity_grant.sql`; the migration is `migrations/00039_identity_grant.sql`.
2. The same plan says direct `impersonator`/`break_glass` claims still populate `Actor` when `GrantID` is absent; current `Verifier.Actor` explicitly ignores them, and `TestActor_DirectImpersonationClaimIgnoredWithoutGrantID` proves it.
3. `staging-validation-plan.md` asks operators to query nonexistent `actor_user_id`, `impersonator_user_id`, and `break_glass` columns.
4. `rollback-plan.md` proposes returning to direct-claim trust/fallback, which is not implemented and would reverse the security authority boundary.
5. The wowsociety reviewer/sign-off remains `TBD`.

Actual result: the framework contract is ready, but **PROD-04 is BLOCKED for cutover** and AC-W07-E03-S001-04 is not satisfied until the rollout artifact is corrected and jointly reviewed.

### D-04 hash version

```text
go test ./kernel/audit ./migrations -run 'TestIntegrationAudit(HashVersionBranching|UnknownHashVersionFailsClosed|ChainDetectsPerFieldTampering)|TestKernelMigrationsHaveManifests' -count=1 -v
```

Result: exit 0. Historical v1 and new v2 rows verify; unknown versions fail closed; every selected widened field tamper breaks verification; the migration manifest is valid.

```text
go test ./internal/cli -run 'TestAudit(NoArgsUsage|UnknownSubcommand|BadTenant|MissingDSN|VerifyEmptyTenantDB|VerifyIntactAfterWriteDB|VerifyDetectsTamperDB)' -count=1 -v
```

Result: exit 0. The product-facing `wowapi audit verify --tenant <uuid>` command's usage/error
contract, intact empty/non-empty chains, and tamper-detected exit path all passed against the required
database. This is the exact per-tenant command named in the consolidated staging path.

The live catalog probe confirms:

```text
hash_version | smallint | 1 | NO
```

Source inspection confirms `migrations/00037_audit_hash_version.sql` is online-classified, new writes use version 2, and verification dispatches by stored version. Actual result: **PROD-05 READY** for the documented all-chain product staging drill.

## Conclusion

This evidence fails AC-W07-E03-S001-04 because the required coordinated rollout artifact is not executable against the current framework. It supports PROD-05 and the framework half of PROD-04 without claiming product cutover readiness.
